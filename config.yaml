# Default update config
# Config order: hparams_registry -> config.yaml -> CLI

# SWAD
swad: LossValley
swad_kwargs:
  n_converge: 3
  n_tolerance: 6
  tolerance_ratio: 0.3
test_batchsize: 128
indomain_test: False  # use indomain test split or not

lr: 5e-5
# Contrast settings
Linear_cls: True
CLIP: False
backbone: RN50 # 'RN50', 'ViT'